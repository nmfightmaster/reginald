{
  "name": "reginald",
  "private": true,
  "packageManager": "pnpm",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --filter @reginald/desktop run tauri dev",
    "build": "pnpm --filter @reginald/desktop run tauri build",
    "typecheck": "pnpm --filter @reginald/desktop exec tsc --noEmit",
    "lint": "echo \"lint not set up yet\""
  }
}